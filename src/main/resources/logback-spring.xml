<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="orders-system"/>
    <springProperty scope="context" name="ACTIVE_PROFILES" source="spring.profiles.active"/>

    <property name="STDOUT_PATTERN" value="%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ} %-5level [%thread] %logger{36} - [%X{correlationId}] %msg%n"/>

    <!-- Encoder for JSON logs -->
    <appender name="JSON" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/application.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/application-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <fieldName>timestamp</fieldName>
                </timestamp>
                <pattern>
                    <pattern>{"level":"%level"}</pattern>
                </pattern>
                <pattern>
                    <pattern>{"logger":"%logger{36}"}</pattern>
                </pattern>
                <pattern>
                    <pattern>{"thread":"%thread"}</pattern>
                </pattern>
                <mdc/>
                <!-- Message: rely on built-in message provider to handle escaping safely -->
                <message/>
                <stackTrace>
                    <fieldName>exception</fieldName>
                </stackTrace>
                <pattern>
                    <pattern>{"service":"${APP_NAME}"}</pattern>
                </pattern>
            </providers>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${STDOUT_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Profile based logging: dev uses console pretty, others use JSON file + console info -->
    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="STDOUT"/>
        </root>
    </springProfile>

    <springProfile name="!dev">
        <root level="INFO">
            <appender-ref ref="JSON"/>
            <appender-ref ref="STDOUT"/>
        </root>
    </springProfile>
</configuration>